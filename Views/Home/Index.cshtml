@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="text-center font-bold font-mono text-4xl">Информационная система компании</h1>
    <div class="mt-20 flex justify-center">
        <a class="px-20 py-3 bg-orange-600 rounded-xl text-white font-bold mx-5 hover:bg-orange-700" href="@Url.Action("Index", "Division")">Список отделов</a>
        <a class="px-20 py-3 bg-blue-600 rounded-xl text-white font-bold mx-5 hover:bg-blue-700" href="@Url.Action("Index", "Employee")">Список сотрудников</a>
    </div>

   
</div>
